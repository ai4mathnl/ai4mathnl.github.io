source "https://rubygems.org"

# Mirrors what GitHub Pages runs, so a local preview matches the live build.
gem "github-pages", group: :jekyll_plugins

gem "webrick"
gem "tzinfo-data"

group :jekyll_plugins do
  gem "jekyll-seo-tag"
end
